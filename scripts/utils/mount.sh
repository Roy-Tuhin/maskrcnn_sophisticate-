#!/bin/bash

##----------------------------------------------------------
### ## AI System setup - mount
##----------------------------------------------------------
##
##----------------------------------------------------------

SCRIPTS_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}")" && pwd )"

source "${SCRIPTS_DIR}/config.mount.local.sh"
